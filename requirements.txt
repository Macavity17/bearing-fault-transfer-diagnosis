# 高速列车轴承智能故障诊断 —— 运行依赖
# 原始环境：Anaconda base, Python 3.11.7（取自 notebook kernel 元数据）
# 说明：原始环境未留下版本记录，以下为按代码实际调用的 API 推定的**最低版本**，非原始锁定版本。
#      如需严格复现，请在跑通后用 `pip freeze > requirements.lock.txt` 固化。

# --- 核心数值与数据处理 ---
numpy>=1.24
pandas>=2.0
scipy>=1.10                # signal.hilbert/butter/filtfilt 包络解调；linalg.sqrtm 用于 CORAL

# --- 建模 ---
scikit-learn>=1.3          # ensemble / model_selection / preprocessing / metrics
                           # feature_selection.RFECV、inspection.permutation_importance、manifold.TSNE
xgboost>=2.0
imbalanced-learn>=0.11     # SMOTE（源域）与 SMOTENC（目标域，混合类别特征）

# --- 可解释性 ---
shap>=0.44                 # TreeExplainer + beeswarm / summary 图

# --- 绘图 ---
matplotlib>=3.7
seaborn>=0.12

# --- 工具 ---
tqdm>=4.65
openpyxl>=3.1              # 读写 data/features/*/dataset.xlsx

# --- 运行环境 ---
jupyter>=1.0
